<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>묵상 편집기 | Bona Studio</title>
    <!-- Google Fonts -->
    <link
        href="https://fonts.googleapis.com/css2?family=Cinzel:wght@400;700&family=Dancing+Script:wght@400;700&family=Noto+Sans+KR:wght@300;400;500;700&family=Noto+Serif+KR:wght@400;700&display=swap"
        rel="stylesheet">
    <!-- Style CSS -->
    <link rel="stylesheet" href="/studio/css/style.css">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <style>
        /* Custom Overrides for Dark UI */
        .text-gold {
            color: #b8860b !important;
            text-shadow: 0 0 5px rgba(184, 134, 11, 0.3);
        }

        /* 컨테이너 좌우 여백 축소 */
        .message-container {
            padding: 1.5rem 1rem;
            text-align: center;
            max-width: 100%;
        }

        /* 제목 섹션 */
        .message-header {
            font-family: 'Cinzel', serif;
            font-size: 1.3rem;
            color: #b8860b;
            font-weight: 700;
            margin-bottom: 0.2rem;
            text-shadow: 0 0 10px rgba(184, 134, 11, 0.4);
        }

        .message-date {
            color: #87ceeb;
            font-size: 0.8rem;
            margin-bottom: 0.8rem;
            font-family: 'Cinzel', serif;
            text-shadow: 0 1px 3px rgba(0, 0, 0, 0.8);
            font-weight: 700;
        }

        /* 복음 핵심 구절 */
        .gospel-quote {
            font-size: 1.05rem;
            font-weight: 700;
            margin-bottom: 0.8rem;
            line-height: 1.35;
            color: #eee;
            padding: 0 0.5rem;
        }

        /* 메인 이미지 */
        .main-image {
            width: 100%;
            max-width: 300px;
            border-radius: 4px;
            border: 1px solid #b8860b;
            box-shadow: 0 0 15px rgba(184, 134, 11, 0.2);
            margin-bottom: 0.2rem;
            cursor: pointer;
        }

        .image-caption {
            color: #666;
            font-size: 0.65rem;
            margin-bottom: 1.2rem;
        }

        /* 시(Poem) 스타일 */
        .poem-content {
            font-size: 0.9rem;
            line-height: 1.5;
            color: #ffffff;
            margin-bottom: 0.8rem;
            white-space: pre-line;
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.8);
        }

        .prayer-text {
            font-style: italic;
            color: #cccccc;
            font-size: 0.8rem;
            margin-bottom: 1rem;
            line-height: 1.3;
            padding: 0 0.5rem;
            word-break: keep-all;
        }

        .phone-frame {
            background: #111;
            border-color: #333;
            height: 900px;
            /* Increased from 812px */
            transform: scale(0.8);
            /* Slightly reduced scale to fit taller frame */
        }

        /* 폰 프레임 내부 패딩 대폭 축소 */
        .preview-content {
            padding: 0.8rem 0.8rem 2.5rem 0.8rem !important;
            /* Added bottom padding */
            display: flex;
            flex-direction: column;
            align-items: center;
            color: white;
            text-align: center;
            height: 100%;
            overflow-y: auto;
            /* Changed from hidden to auto */
            position: relative;
        }

        /* 1. Date */
        .prev-date {
            font-family: 'Cinzel', serif;
            color: #888;
            margin-top: 1rem;
            font-size: 0.9rem;
        }

        /* 2. Headline Ref */
        #prevRef {
            font-family: 'Noto Serif KR', serif;
            color: #aaa;
            margin-top: 1.5rem;
            font-size: 0.9rem;
        }

        /* 3. One Line Message */
        #prevTitle {
            font-family: 'Noto Serif KR', serif;
            font-size: 1.3rem;
            margin: 0.8rem 0 1.2rem 0;
            font-weight: 700;
            line-height: 1.4;
            word-break: keep-all;
        }

        /* 시(Poem) 스타일 - 가독성 개선 */
        .poem-content {
            font-size: 1rem;
            line-height: 1.7;
            color: #ffffff;
            margin-bottom: 1.5rem;
            white-space: pre-line;
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.8);
        }

        .prayer-text {
            font-style: italic;
            color: #cccccc;
            font-size: 0.9rem;
            margin-bottom: 1.5rem;
            line-height: 1.5;
        }

        /* 4. Gold Frame (Landscape) */
        .gold-frame-container {
            margin: 0.5rem 0 1.5rem 0;
            cursor: pointer;
            transition: transform 0.2s;
            width: 100%;
            display: flex;
            justify-content: center;
        }

        .gold-frame-container:hover {
            transform: scale(1.02);
        }

        .gold-frame {
            border: 4px solid #b8860b;
            border-radius: 4px;
            padding: 4px;
            background: #fff;
            box-shadow: 0 0 15px rgba(184, 134, 11, 0.4);
            display: inline-block;
            line-height: 0;
            width: 100%;
            max-width: 90%;
        }

        .gold-frame img {
            width: 100%;
            height: auto;
            aspect-ratio: 16 / 9;
            object-fit: cover;
            display: block;
        }

        /* 5. Meditation Poem */
        #prevBody {
            font-family: 'Noto Serif KR', serif;
            font-size: 0.95rem;
            line-height: 1.8;
            margin-bottom: 2rem;
            white-space: pre-wrap;
            opacity: 0.9;
            text-align: center;
            flex: 1;
        }

        /* 6. Prayer */
        #prevPrayer {
            font-family: 'Noto Sans KR', sans-serif;
            font-size: 0.85rem;
            color: #ccc;
            margin-bottom: 1.5rem;
            padding-top: 1rem;
            border-top: 1px solid rgba(255, 255, 255, 0.2);
            width: 80%;
            font-style: italic;
        }

        /* Footer */
        .prev-footer {
            font-family: 'Cinzel';
            font-size: 0.8rem;
            letter-spacing: 2px;
            color: #555;
            margin-bottom: 2rem;
        }

        /* Modal Styles */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            z-index: 2000;
            display: none;
            justify-content: center;
            align-items: center;
            flex-direction: column;
        }

        .modal-overlay.open {
            display: flex;
        }

        .modal-content {
            position: relative;
            max-width: 90%;
            max-height: 90%;
            text-align: center;
        }

        .modal-close {
            position: absolute;
            top: -50px;
            right: 0;
            background: none;
            border: 1px solid #fff;
            color: #fff;
            padding: 5px 15px;
            border-radius: 20px;
            cursor: pointer;
            font-size: 1rem;
        }

        .modal-close:hover {
            background: white;
            color: black;
        }

        .gold-frame.large {
            border-width: 8px;
            padding: 8px;
            background: #000;
            max-width: none;
        }

        .gold-frame.large img {
            max-height: 80vh;
            max-width: 90vw;
            width: auto;
            height: auto;
            aspect-ratio: auto;
        }

        .missa-link-btn {
            display: inline-block;
            padding: 0.6rem 1.2rem;
            border: 1px solid #444;
            border-radius: 30px;
            color: #888;
            text-decoration: none;
            font-size: 0.8rem;
            transition: all 0.3s;
            margin-bottom: 0.5rem;
            background: rgba(255, 255, 255, 0.05);
        }

        .missa-link-btn:hover {
            border-color: #b8860b;
            color: #b8860b;
            background: rgba(184, 134, 11, 0.1);
        }
    </style>
</head>

<body>
    <% if (typeof data !=='undefined' && data && data.content) { Object.assign(data, data.content); } %>
        <div class="glass-container" style="flex-direction: row; width: 98%;">
            <!-- 좌측: 입력 패널 -->
            <div class="editor-left">
                <div style="display: flex; justify-content: space-between; margin-bottom: 2rem;">
                    <button onclick="location.href='/studio'" class="btn-secondary"><i
                            class="fa-solid fa-arrow-left"></i>
                        돌아가기</button>
                    <div style="font-weight: 700; font-size: 1.2rem; color: var(--primary-color);">
                        <%= date %>
                    </div>
                    <button id="toggleMissa" class="btn-secondary" style="color: #2980b9;"><i
                            class="fa-solid fa-book-bible"></i> 매일미사 보기</button>
                </div>

                <!-- Date Navigation (Simple Text Only) -->
                <div
                    style="display: flex; gap: 20px; justify-content: center; margin-bottom: 2rem; align-items: center;">
                    <button onclick="moveDate(-1)"
                        style="background: none; border: none; color: #888; font-size: 1.2rem; cursor: pointer; font-weight: bold; padding: 5px;">&lt;&lt;&lt;</button>
                    <button onclick="goToDate('today')"
                        style="background: none; border: none; color: #b8860b; font-size: 1.1rem; cursor: pointer; font-weight: bold; border-bottom: 1px solid #b8860b; padding-bottom: 2px;">오늘</button>
                    <button onclick="moveDate(1)"
                        style="background: none; border: none; color: #888; font-size: 1.2rem; cursor: pointer; font-weight: bold; padding: 5px;">&gt;&gt;&gt;</button>
                </div>

                <form id="editorForm">
                    <!-- Action Buttons -->
                    <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-bottom: 2rem;">
                        <button type="button" onclick="generateDraft()" class="btn-primary"
                            style="background: #eebb11; color: #000; font-weight: 800;">
                            ✨ 묵상초안 생성하기 (AI)
                        </button>
                        <button type="button" onclick="saveDraft()" class="btn-secondary">
                            💾 저장하기 (Save)
                        </button>
                    </div>

                    <div class="input-group">
                        <label>복음 (Headline Ref)</label>
                        <input type="text" class="input-control" name="headline_ref" placeholder="예: 루카 10,1-9"
                            value="<%= data.headline_ref || '' %>">
                    </div>

                    <div class="input-group">
                        <label>오늘의 말씀 (Theme)</label>
                        <input type="text" class="input-control" name="one_line_message"
                            placeholder="예: 수확할 것은 많은데 일꾼은 적다." value="<%= data.one_line_message || '' %>">
                    </div>

                    <div class="input-group">
                        <label>묵상 (Meditation Poem)</label>
                        <textarea class="input-control" name="meditation_body" style="min-height: 240px;"
                            placeholder="AI가 생성한 6줄 내외의 시가 여기에 들어갑니다."><%= data.meditation_body || '' %></textarea>
                    </div>

                    <div class="input-group">
                        <label>오늘의 기도 (Prayer)</label>
                        <input type="text" class="input-control" name="prayer_line" placeholder="예: 주님, 당신의 뜻을 따르게 하소서."
                            value="<%= data.prayer_line || '' %>">
                    </div>

                    <div class="input-group">
                        <label>AI 그림 프롬프트 (가로형 풍경화)</label>
                        <textarea class="input-control" name="image_prompt_scenery" style="min-height: 80px;" disabled
                            placeholder="AI가 생성한 이미지 프롬프트가 여기에 표시됩니다."></textarea>
                    </div>

                    <hr style="border: 0; border-top: 1px solid #ddd; margin: 2rem 0;">

                    <button type="button" onclick="openPublishModal()" class="btn-primary" style="margin-top: 1rem;">
                        <i class="fa-solid fa-rocket"></i> 배포 센터 (Deployment Center)
                    </button>
                </form>
            </div>

            <!-- 우측: 미리보기 -->
            <div class="editor-right">
                <div class="phone-frame">
                    <div class="preview-content">

                        <!-- NEW: App Title (Top) -->
                        <div
                            style="font-family: 'Cinzel', serif; font-size: 1.4rem; color: #b8860b; font-weight: 700; margin-top: 1rem; margin-bottom: 0.5rem; letter-spacing: 1px;">
                            Good Morning Bona
                        </div>

                        <!-- 1. 날짜 -->
                        <div class="prev-date" id="previewDate"
                            style="color: #b8860b; font-size: 0.85rem; margin-top: 0; font-weight: 400; text-shadow: 0 0 8px rgba(184, 134, 11, 0.3);">
                        </div>

                        <!-- 2. 복음 구절 (삭제요청으로 숨김) -->
                        <div id="prevRef" style="display:none;"></div>

                        <!-- 3. 오늘의 말씀 -->
                        <div id="prevTitle" style="margin-top: 1.5rem;">
                            오늘의 말씀을 기다립니다
                        </div>

                        <!-- 4. 금장 액자 이미지 (가로형) -->
                        <div class="gold-frame-container" onclick="openImageModal()">
                            <div class="gold-frame">
                                <img id="prevImage" src="https://picsum.photos/800/450?random=1" alt="Meditation Image">
                            </div>
                        </div>

                        <!-- 5. 묵상 시 -->
                        <div id="prevBody" style="margin-bottom: 1rem;">
                            "버튼을 눌러 묵상을 생성해주세요."
                        </div>

                        <!-- 6. 오늘의 기도 -->
                        <div id="prevPrayer" style="margin-bottom: 1.5rem; border-top: none; padding-top: 0;">
                            - Bona Studio -
                        </div>

                        <!-- 7. 버튼 행 -->
                        <div
                            style="display: flex; gap: 0.8rem; justify-content: center; width: 100%; margin-bottom: 1.5rem; margin-top: 1.5rem;">
                            <a href="https://missa.cbck.or.kr/DailyMissa" target="_blank"
                                style="flex: 1; max-width: 180px; padding: 0.35rem 0.5rem; border: 1px solid #b8860b; border-radius: 30px; color: #b8860b; text-decoration: none; font-size: 0.75rem; background: none; text-align: center;">
                                매일미사 전문 보기
                            </a>
                            <button onclick="openSubModal()"
                                style="flex: 1; max-width: 180px; padding: 0.35rem 0.5rem; border: 1px solid #b8860b; border-radius: 30px; color: #b8860b; background: none; font-size: 0.75rem; text-align: center; cursor: pointer;">
                                매일아침 묵상카드 받기
                            </button>
                        </div>

                        <!-- Footer -->
                        <div class="prev-footer"
                            style="font-family: 'Dancing Script', cursive; color: #b8860b; font-size: 1.2rem; margin-top: auto; text-transform: none; letter-spacing: 0;">
                            Bona Studio
                        </div>

                        <!-- Share Button -->
                        <button
                            style="width: 100%; background: none; border: 1px solid #b8860b; color: #b8860b; padding: 0.35rem 1rem; font-size: 0.75rem; border-radius: 30px; cursor: pointer; margin-top: 1rem;">
                            전달하기
                        </button>
                    </div>
                    <div class="prev-footer" style="color: #b8860b; font-weight: 700; margin-top: auto;">
                        GOOD MORNING BONA
                    </div>
                </div>
            </div>

            <!-- (기존 미리보기 안내 텍스트) -->
            <div style="position: absolute; bottom: 1rem; color: #888; font-size: 0.8rem; text-align: center;">
                * 미리보기 화면입니다. 실시간으로 반영됩니다.
            </div>
        </div>

        <!-- 구독 신청 모달 (Floating Modal) -->
        <div id="subModal" class="modal-overlay" onclick="closeSubModal()">
            <div class="modal-card" onclick="event.stopPropagation()">
                <button class="modal-close-btn" onclick="closeSubModal()">✕</button>

                <div id="subWelcome">
                    <div style="font-family: 'Cinzel'; font-size: 1.5rem; color: #b8860b; margin-bottom: 0.5rem;">Good
                        Morning Bona</div>
                    <p
                        style="color: #ccc; font-size: 0.95rem; margin-bottom: 2rem; font-family: 'Noto Sans KR', sans-serif; line-height: 1.6; word-break: keep-all;">
                        [가톨릭] 매일미사 복음<br>묵상카드를 받아보세요
                    </p>
                    <button class="sub-action-btn" onclick="startSubscription()">
                        <i class="fa-solid fa-envelope-open-text"></i> 매일아침 묵상카드 받기
                    </button>
                </div>

                <div id="subSuccess" style="display: none;">
                    <div style="font-size: 3rem; color: #b8860b; margin-bottom: 1rem;"><i
                            class="fa-regular fa-circle-check"></i></div>
                    <div style="font-size: 1.2rem; font-weight: bold; margin-bottom: 0.5rem; color: #b8860b;">구독 완료!
                    </div>
                    <button class="sub-action-btn" onclick="closeSubModal()" style="margin-top: 1rem;">닫기</button>
                </div>
            </div>
        </div>

        <style>
            /* 모달 스타일 추가 */
            .modal-card {
                background: #111;
                width: 90%;
                max-width: 340px;
                border-radius: 20px;
                padding: 2rem;
                text-align: center;
                position: relative;
                border: 1px solid #b8860b;
                box-shadow: 0 20px 50px rgba(0, 0, 0, 0.8);
                animation: slideUp 0.3s ease-out;
            }

            @keyframes slideUp {
                from {
                    transform: translateY(50px);
                    opacity: 0;
                }

                to {
                    transform: translateY(0);
                    opacity: 1;
                }
            }

            .modal-close-btn {
                position: absolute;
                top: 15px;
                right: 15px;
                background: none;
                border: none;
                color: #666;
                font-size: 1.2rem;
                cursor: pointer;
            }

            .modal-close-btn:hover {
                color: #fff;
            }

            .sub-action-btn {
                background: linear-gradient(135deg, #b8860b 0%, #daa520 100%);
                border: none;
                color: #000;
                padding: 0.8rem 2rem;
                border-radius: 50px;
                font-weight: bold;
                cursor: pointer;
                width: 100%;
                display: flex;
                align-items: center;
                justify-content: center;
                gap: 8px;
            }
        </style>

        <!-- 이미지 확대 모달 -->
        <div id="imageModal" class="modal-overlay" onclick="closeImageModal()">
            <div class="modal-content" onclick="event.stopPropagation()">
                <button class="modal-close" onclick="closeImageModal()">✕ 닫기</button>
                <div class="gold-frame large">
                    <img id="modalImage" src="" alt="Expanded Image">
                </div>
            </div>
        </div>

        <!-- 매일미사 슬라이딩 패널 -->
        <div class="missa-drawer" id="missaDrawer">
            <div style="padding: 1rem; border-bottom: 1px solid #eee; display: flex; justify-content: space-between;">
                <h3>매일미사 (Original)</h3>
                <button id="closeMissa"
                    style="border: none; background: none; font-size: 1.2rem; cursor: pointer;">✕</button>
            </div>
            <iframe id="missaIframe" src="https://missa.cbck.or.kr/DailyMissa/<%= date.replace(/-/g, '') %>"
                style="width: 100%; height: calc(100% - 60px); border: none;"></iframe>
        </div>

        <!-- 배포 제어 모달 -->
        <div id="publishModal" class="modal-overlay">
            <div class="modal-card" style="border-color: #2e86de; box-shadow: 0 0 30px rgba(46,134,222,0.3);">
                <button class="modal-close-btn" onclick="closePublishModal()">✕</button>
                <h2 style="color: #2e86de; margin-bottom: 0.5rem;">배포 제어 센터</h2>
                <p style="color: #aaa; font-size: 0.9rem; margin-bottom: 2rem;">
                    작성한 묵상을 저장하고 발송합니다.<br>먼저 테스트 발송으로 확인하는 것을 권장합니다.
                </p>

                <!-- 관리자 기기 등록 섹션 -->
                <div style="margin-bottom: 2rem; border-bottom: 1px solid #333; padding-bottom: 1rem;">
                    <button onclick="registerAsAdmin()"
                        style="background: none; border: 1px dashed #555; color: #888; font-size: 0.8rem; padding: 0.5rem 1rem; border-radius: 5px; cursor: pointer;">
                        <i class="fa-solid fa-user-shield"></i> 현재 기기를 [관리자 테스트용]으로 등록
                    </button>
                </div>

                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
                    <!-- 1. 테스트 발송 -->
                    <button onclick="publishTest()"
                        style="background: #333; border: 1px solid #555; color: #fff; padding: 1rem; border-radius: 10px; cursor: pointer;">
                        <i class="fa-solid fa-microchip"></i><br>
                        <span style="font-size: 0.9rem; display: block; margin-top: 5px;">관리자 폰으로<br>시험 발송</span>
                    </button>

                    <!-- 2. 전체 배포 -->
                    <button onclick="publishReal()"
                        style="background: linear-gradient(135deg, #2e86de 0%, #54a0ff 100%); border: none; color: #fff; padding: 1rem; border-radius: 10px; cursor: pointer;">
                        <i class="fa-solid fa-paper-plane"></i><br>
                        <span style="font-size: 0.9rem; display: block; margin-top: 5px;">구독자 전체<br>실전 배포</span>
                    </button>
                </div>

                <div id="pubResult" style="margin-top: 1.5rem; font-size: 0.9rem; color: #ddd; display: none;"></div>
            </div>
        </div>

        <!-- MAIN SCRIPT BLOCK -->
        <script>
            // --- 1. UI Interaction Logic ---
            const toggleBtn = document.getElementById('toggleMissa');
            const closeBtn = document.getElementById('closeMissa');
            const drawer = document.getElementById('missaDrawer');

            function openImageModal() {
                const src = document.getElementById('prevImage').src;
                document.getElementById('modalImage').src = src;
                document.getElementById('imageModal').classList.add('open');
            }
            window.closeImageModal = function () {
                document.getElementById('imageModal').classList.remove('open');
            }

            if (toggleBtn) toggleBtn.addEventListener('click', () => drawer.classList.add('open'));
            if (closeBtn) closeBtn.addEventListener('click', () => drawer.classList.remove('open'));

            // --- 2. Live Preview Logic ---
            const form = document.getElementById('bonaForm');
            const prevRef = document.getElementById('prevRef');
            const prevTitle = document.getElementById('prevTitle');
            const prevBody = document.getElementById('prevBody');
            const prevPrayer = document.getElementById('prevPrayer');

            function updatePreview() {
                if (!form) return;
                if (form.headline_ref.value) prevRef.innerText = form.headline_ref.value;
                if (form.one_line_message.value) prevTitle.innerText = form.one_line_message.value;
                if (form.meditation_body.value) prevBody.innerText = form.meditation_body.value;
                if (form.prayer_line.value) prevPrayer.innerText = form.prayer_line.value;
            }

            // Format and display preview date
            function updatePreviewDate() {
                const dateStr = '<%= date %>';
                if (dateStr) {
                    const dateParts = dateStr.split('-'); // "2026-01-28" -> ["2026", "01", "28"]
                    const formattedDate = `✠ 가톨릭 매일아침 매일미사 복음묵상 ${dateParts[0]}. ${dateParts[1]}. ${dateParts[2]}.`;
                    document.getElementById('previewDate').textContent = formattedDate;
                }
            }

            if (form) {
                form.addEventListener('input', updatePreview);
                updatePreview();
                updatePreviewDate(); // Initialize date on page load
            }

            // --- 3. Subscribe Logic ---
            function openSubModal() { document.getElementById('subModal').classList.add('open'); }
            function closeSubModal() { document.getElementById('subModal').classList.remove('open'); }

            const VAPID_KEY = 'BMz08ooHZoSWB5V1pAX40SPu2LLJ5tFIUO2UlOcpXQ5QHysvenzqGlFzHfznO8WdVt9ocYOcQsTrPui0SXCi1Yw';

            function urlBase64ToUint8Array(base64String) {
                const padding = '='.repeat((4 - base64String.length % 4) % 4);
                const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');
                const rawData = window.atob(base64);
                const outputArray = new Uint8Array(rawData.length);
                for (let i = 0; i < rawData.length; ++i) { outputArray[i] = rawData.charCodeAt(i); }
                return outputArray;
            }

            async function startSubscription() {
                if (!('serviceWorker' in navigator)) return alert('이 브라우저는 지원하지 않습니다.');
                try {
                    const reg = await navigator.serviceWorker.register('/bona/sw.js', { scope: '/bona/' });
                    const sub = await reg.pushManager.subscribe({
                        userVisibleOnly: true,
                        applicationServerKey: urlBase64ToUint8Array(VAPID_KEY)
                    });
                    await fetch('/studio/api/subscribe', {
                        method: 'POST',
                        body: JSON.stringify(sub),
                        headers: { 'Content-Type': 'application/json' }
                    });
                    document.getElementById('subWelcome').style.display = 'none';
                    document.getElementById('subSuccess').style.display = 'block';
                } catch (e) {
                    console.error(e);
                    alert('알림 권한을 허용해주세요.');
                }
            }

            // --- 4. Deployment Logic ---
            let currentImageUrl = ''; // Track the current generated image URL

            function openPublishModal() { document.getElementById('publishModal').style.display = 'flex'; }
            function closePublishModal() {
                document.getElementById('publishModal').style.display = 'none';
                document.getElementById('pubResult').style.display = 'none';
            }

            async function getFormData() {
                return {
                    date: '<%= date %>',
                    headline_ref: form.headline_ref.value,
                    one_line_message: form.one_line_message.value,
                    meditation_body: form.meditation_body.value,
                    prayer_line: form.prayer_line.value,
                    image_prompt_scenery: form.image_prompt_scenery.value,
                    generated_image_url: currentImageUrl || document.getElementById('prevImage').src
                };
            }

            async function registerAsAdmin() {
                if (!confirm('현재 이 브라우저를 [관리자 테스트 기기]로 등록하시겠습니까?\n(기존 관리자 기기는 덮어씌워집니다)')) return;
                if (!('serviceWorker' in navigator)) return alert('알림 미지원');
                try {
                    const reg = await navigator.serviceWorker.ready;
                    let sub = await reg.pushManager.getSubscription();
                    if (!sub) return alert('먼저 묵상카드 구독을 해주셔야 기기 정보를 가져올 수 있습니다.');
                    const res = await fetch('/studio/api/register-admin', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(sub)
                    });
                    const json = await res.json();
                    if (json.success) alert('✅ 등록 완료! 이제 [시험 발송]은 이 기기로만 옵니다.');
                    else alert('등록 실패: ' + json.error);
                } catch (e) { alert('오류: ' + e.message); }
            }

            async function publishTest() {
                const resultDiv = document.getElementById('pubResult');
                resultDiv.style.display = 'block';
                resultDiv.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> 저장 및 [관리자용] 발송 중...';
                try {
                    const data = await getFormData();
                    const res = await fetch('/studio/api/publish?test=true', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(data)
                    });
                    const json = await res.json();
                    if (json.success) {
                        resultDiv.innerHTML = `<span style="color: #55efc4">✅ 전송 성공!<br>관리자 기기로 알림을 보냈습니다.</span>`;
                    } else {
                        if (json.error && json.error.includes('관리자')) {
                            resultDiv.innerHTML = `<span style="color: #ff6b6b">⚠️ ${json.error}</span>`;
                        } else {
                            resultDiv.innerHTML = `<span style="color: #ff6b6b">⚠️ 실패: ${json.error}</span>`;
                        }
                    }
                } catch (e) { console.error(e); resultDiv.innerText = '통신 오류'; }
            }

            async function publishReal() {
                if (!confirm('🚨 정말로 모든 구독자에게 알림을 발송하시겠습니까?')) return;
                const resultDiv = document.getElementById('pubResult');
                resultDiv.style.display = 'block';
                resultDiv.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> 전체 발송 중...';
                try {
                    const data = await getFormData();
                    const res = await fetch('/studio/api/publish', {
                        method: 'POST',
                        headers: { 'Content-Type': 'application/json' },
                        body: JSON.stringify(data)
                    });
                    const json = await res.json();
                    if (json.success) {
                        resultDiv.innerHTML = `<span style="color: #55efc4">🎉 <b>배포 완료!</b><br>총 ${json.subscriberCount}명에게 발송했습니다.</span>`;
                    } else {
                        resultDiv.innerHTML = '실패: ' + json.error;
                    }
                } catch (e) { console.error(e); resultDiv.innerText = '통신 오류'; }
            }

            // --- 5. AI Generation ---
            const aiBtn = document.getElementById('aiGenBtn');

            // 모달 열기/닫기 함수
            function openAiConfirmModal() {
                document.getElementById('aiConfirmModal').style.display = 'flex';
            }

            window.closeAiConfirmModal = function () {
                document.getElementById('aiConfirmModal').style.display = 'none';
            }

            // AI 생성 실행 함수
            window.confirmAiGeneration = function () {
                closeAiConfirmModal();

                const originalText = aiBtn.innerHTML;
                aiBtn.innerHTML = '<i class="fa-solid fa-spinner fa-spin"></i> 생성 중... (약 10초)';
                aiBtn.disabled = true;

                fetch('/studio/api/draft', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ date: '<%= date %>' })
                })
                    .then(res => res.json())
                    .then(json => {
                        if (json.success && json.data && json.data.content) {
                            const data = json.data.content;
                            form.headline_ref.value = data.headline_ref || '';
                            form.one_line_message.value = data.one_line_message || '';
                            form.meditation_body.value = data.meditation_body || '';
                            form.prayer_line.value = data.prayer_line || '';
                            if (data.image_prompt_scenery) form.image_prompt_scenery.value = data.image_prompt_scenery;
                            if (data.generated_image_url) {
                                currentImageUrl = data.generated_image_url; // Save to global variable
                                document.getElementById('prevImage').src = data.generated_image_url;
                            }
                            updatePreview();
                        } else {
                            alert('생성 실패: ' + (json.error || '알 수 없는 오류'));
                        }
                    })
                    .catch(e => {
                        alert('요청 중 오류 발생: ' + e.message);
                    })
                    .finally(() => {
                        aiBtn.innerHTML = originalText;
                        aiBtn.disabled = false;
                    });
            }

            // AI 버튼 클릭 시 모달 열기
            if (aiBtn) {
                aiBtn.addEventListener('click', () => {
                    openAiConfirmModal();
                });
            }

            // --- 페이지 로드 시 복음 데이터 자동 로드 ---
            async function loadGospelData() {
                // 이미 저장된 데이터가 있으면 스킵
                if (form.headline_ref.value || form.one_line_message.value) {
                    console.log('[Gospel] 이미 데이터가 있어서 스킵');
                    return;
                }

                try {
                    const date = '<%= date %>';
                    console.log(`[Gospel] ${date} 복음 데이터 로딩...`);

                    const res = await fetch(`/studio/api/gospel/${date}`);
                    const json = await res.json();

                    if (json.success && json.data) {
                        // 오늘의 말씀과 성경 구절 자동 입력
                        form.headline_ref.value = json.data.reference || '';
                        form.one_line_message.value = json.data.title || '';

                        updatePreview();
                        console.log('[Gospel] 복음 데이터 로드 완료:', json.data.reference);
                    } else {
                        console.log('[Gospel] 복음 데이터 없음');
                    }
                } catch (e) {
                    console.error('[Gospel] 로드 실패:', e);
                }
            }

            // 페이지 로드 시 자동 실행
            if (form) {
                loadGospelData();

                // 저장된 이미지 URL 로드
                <% if (typeof data !== 'undefined' && data && data.generated_image_url) { %>
                    currentImageUrl = '<%= data.generated_image_url %>';
                    document.getElementById('prevImage').src = '<%= data.generated_image_url %>';
                    updatePreview();
                <% } %>
            }
        </script>

        <!-- AI 생성 확인 모달 -->
        <div id="aiConfirmModal" class="modal-overlay">
            <div class="modal-card" style="border-color: #6a11cb; box-shadow: 0 0 30px rgba(106,17,203,0.3);">
                <button class="modal-close-btn" onclick="closeAiConfirmModal()">✕</button>
                <div style="font-size: 2.5rem; color: #6a11cb; margin-bottom: 1rem;">
                    <i class="fa-solid fa-wand-magic-sparkles"></i>
                </div>
                <h2 style="color: #6a11cb; margin-bottom: 0.5rem;">AI 묵상 초안 생성</h2>
                <p style="color: #aaa; font-size: 0.9rem; margin-bottom: 2rem; line-height: 1.6;">
                    AI가 오늘의 복음을 바탕으로<br>
                    묵상, 기도, 이미지 프롬프트를 생성합니다.<br>
                    <span style="color: #ff6b6b; font-weight: bold;">기존 내용은 덮어씌워집니다.</span>
                </p>

                <div style="display: grid; grid-template-columns: 1fr 1fr; gap: 1rem;">
                    <button onclick="closeAiConfirmModal()"
                        style="background: #333; border: 1px solid #555; color: #fff; padding: 0.8rem; border-radius: 10px; cursor: pointer;">
                        취소
                    </button>
                    <button onclick="confirmAiGeneration()"
                        style="background: linear-gradient(135deg, #6a11cb 0%, #2575fc 100%); border: none; color: #fff; padding: 0.8rem; border-radius: 10px; cursor: pointer; font-weight: bold;">
                        생성하기
                    </button>
                </div>
            </div>
        </div>
</body>

</html>