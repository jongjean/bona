<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>Bona Studio - 매일아침 복음묵상</title>
    <!-- KakaoTalk & Social Share Meta Tags (SSR) -->
    <meta property="og:title" content="<%= metaData ? metaData.title : 'Good Morning Bona' %>">
    <meta property="og:description" content="<%= metaData ? metaData.description : '(가톨릭) 매일아침 매일미사 복음묵상' %>">
    <meta property="og:image" content="<%= metaData ? metaData.image : 'https://uconai.ddns.net/bona/logo.png' %>">
    <meta property="og:image:width" content="1200">
    <meta property="og:image:height" content="630">
    <meta property="og:url" content="<%= metaData ? metaData.url : 'https://uconai.ddns.net/bona/' %>">
    <meta property="og:type" content="website">
    <!-- Google Fonts -->
    <link
        href="https://fonts.googleapis.com/css2?family=Noto+Serif+KR:wght@400;700&family=Dancing+Script:wght@700&family=Noto+Sans+KR:wght@300;400;700&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">

    <style>
        /* Base Reset */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            -webkit-tap-highlight-color: transparent;
        }

        body {
            background-color: #000;
            color: #fff;
            font-family: 'Noto Sans KR', sans-serif;
            display: flex;
            justify-content: center;
            min-height: 100vh;
            overflow-x: hidden;
            padding-bottom: 2rem;
        }

        /* Container */
        .card-container {
            width: 100%;
            max-width: 420px;
            background: #000;
            padding: 1.5rem;
            display: flex;
            flex-direction: column;
            align-items: center;
            min-height: 100vh;
            position: relative;
        }

        /* Header */
        .app-title {
            font-family: 'Dancing Script', cursive;
            color: #b8860b;
            font-size: 1.8rem;
            margin-bottom: 1.5rem;
            margin-top: 0.5rem;
            letter-spacing: 1px;
            text-shadow: 0 0 10px rgba(184, 134, 11, 0.3);
        }

        /* Date */
        .date-display {
            color: #b8860b;
            font-size: 0.8rem;
            margin-bottom: 1rem;
            font-family: 'Noto Sans KR', sans-serif;
            font-weight: 300;
            text-align: center;
            width: 100%;
        }

        /* Gospel Message (Title) */
        .gospel-title {
            font-family: 'Noto Serif KR', serif;
            font-size: 1.3rem;
            font-weight: 700;
            line-height: 1.4;
            margin-bottom: 1.5rem;
            text-align: center;
            word-break: keep-all;
            color: #fff;
        }

        /* Image Frame */
        .gold-frame-container {
            margin: 1.5rem 0;
            display: flex;
            justify-content: center;
            width: 100%;
        }

        .gold-frame {
            border: 2px solid #b8860b;
            padding: 4px;
            border-radius: 4px;
            box-shadow: 0 0 15px rgba(184, 134, 11, 0.2);
            width: 100%;
            max-width: 100%;
        }

        .gold-frame img {
            width: 100%;
            height: auto;
            display: block;
            border-radius: 2px;
            filter: contrast(1.05) brightness(1.05);
            cursor: pointer;
        }

        /* Section Labels */
        .section-label {
            width: 100%;
            text-align: center;
            color: #b8860b;
            font-size: 0.8rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
            font-family: 'Noto Sans KR', sans-serif;
            margin-top: 1.5rem;
            margin-left: auto;
            margin-right: auto;
        }

        /* Body Text */
        .meditation-body {
            font-family: 'Noto Serif KR', serif;
            font-size: 1rem;
            line-height: 1.8;
            margin-bottom: 2rem;
            white-space: pre-wrap;
            opacity: 0.9;
            text-align: center;
            color: #fff;
            padding: 0 0.5rem;
            width: 100%;
            margin-left: auto;
            margin-right: auto;
        }

        /* Prayer Text */
        .prayer-text {
            font-family: 'Noto Serif KR', serif;
            font-style: italic;
            color: #cccccc;
            font-size: 0.9rem;
            margin-bottom: 2rem;
            line-height: 1.5;
            padding-top: 1rem;
            border-top: 1px solid rgba(255, 255, 255, 0.2);
            width: 80%;
            text-align: center;
            margin-left: auto;
            margin-right: auto;
            /* Editable Style */
            outline: none;
        }

        .prayer-text:focus {
            border-bottom: 1px dashed rgba(184, 134, 11, 0.5);
        }

        /* Content Wrapper */
        .content-wrapper {
            width: 100%;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        .fixed-footer-wrapper {
            width: 100%;
            margin-top: 2rem;
            padding-bottom: 1rem;
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        /* Buttons */
        .button-row {
            display: flex;
            gap: 0.8rem;
            justify-content: center;
            width: 100%;
            margin-bottom: 1.5rem;
        }

        .missa-link-btn,
        .subscribe-btn {
            flex: 1;
            max-width: 180px;
            padding: 0.5rem;
            border: 1px solid #b8860b;
            border-radius: 30px;
            color: #b8860b;
            text-decoration: none;
            font-size: 0.8rem;
            background: none;
            text-align: center;
            cursor: pointer;
            font-family: 'Noto Sans KR', sans-serif;
            transition: all 0.2s;
        }

        .subscribe-btn:active,
        .missa-link-btn:active {
            background: rgba(184, 134, 11, 0.2);
        }

        /* Footer Text */
        .footer {
            font-family: 'Dancing Script', cursive;
            color: #b8860b;
            font-size: 1.4rem;
            margin-bottom: 1rem;
        }

        /* Share Button */
        .share-btn {
            width: 100%;
            background: none;
            border: 1px solid #b8860b;
            color: #b8860b;
            padding: 0.4rem 1rem;
            font-size: 0.8rem;
            border-radius: 30px;
            cursor: pointer;
            margin-top: 0.5rem;
            font-family: 'Noto Sans KR', sans-serif;
        }

        /* Utility */
        .loading {
            color: #888;
            margin: auto;
            display: flex;
            flex-direction: column;
            gap: 1rem;
            text-align: center;
        }

        .error {
            color: #ff6b6b;
            font-size: 0.9rem;
            margin: auto;
            text-align: center;
        }

        .hidden {
            display: none !important;
        }

        /* Modal Styles */
        .modal-overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.85);
            backdrop-filter: blur(5px);
            display: flex;
            justify-content: center;
            align-items: center;
            z-index: 2000;
        }

        .modal-card {
            background: #1a1a1a;
            border: 1px solid #b8860b;
            border-radius: 24px;
            width: 90%;
            max-width: 320px;
            padding: 2rem 1.5rem;
            text-align: center;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
            animation: modalFadeIn 0.3s cubic-bezier(0.16, 1, 0.3, 1);
        }

        @keyframes modalFadeIn {
            from {
                opacity: 0;
                transform: translateY(10px) scale(0.95);
            }

            to {
                opacity: 1;
                transform: translateY(0) scale(1);
            }
        }

        /* Image Zoom Modal */
        .image-modal {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.95);
            z-index: 10000;
            display: none;
            justify-content: center;
            align-items: center;
            backdrop-filter: blur(5px);
        }

        .image-modal img {
            max-width: 95%;
            max-height: 95%;
            box-shadow: 0 0 20px rgba(0, 0, 0, 0.5);
            border: 2px solid #b8860b;
        }

        /* Kakao/Notif Floating Guide */
        .kakao-guide {
            position: fixed;
            bottom: 80px;
            left: 50%;
            transform: translateX(-50%);
            width: 90%;
            max-width: 400px;
            background: rgba(20, 20, 20, 0.95);
            border: 2px solid #b8860b;
            border-radius: 15px;
            padding: 1.2rem;
            z-index: 9999;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.5);
            text-align: center;
            animation: slideUp 0.5s ease-out;
            display: none;
        }

        .kakao-guide p {
            color: #ccc;
            font-size: 0.9rem;
            margin-bottom: 0;
            line-height: 1.5;
        }

        .kakao-guide h3 {
            color: #b8860b;
            font-size: 1rem;
            margin-bottom: 8px;
            font-weight: bold;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translate(-50%, 20px);
            }

            to {
                opacity: 1;
                transform: translate(-50%, 0);
            }
        }
    </style>
</head>

<body>
    <!-- Image Zoom Modal -->
    <div id="imageModal" class="image-modal" onclick="this.style.display='none'">
        <img id="zoomedImage" src="">
    </div>

    <!-- Custom Share Modal -->
    <div id="shareModal" class="modal-overlay hidden" onclick="if(event.target === this) closeShareModal()">
        <div class="modal-card" style="padding: 1.5rem; max-width: 300px;">
            <h3 style="color: #b8860b; margin-bottom: 1.5rem; font-size: 1.1rem;">어디로 전달할까요?</h3>
            <div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 0.5rem;">
                <!-- KakaoTalk -->
                <button onclick="handleShare('kakao')"
                    style="background:none; border:none; cursor:pointer; color:#fff;">
                    <div
                        style="background:#FEE500; width:50px; height:50px; border-radius:50%; margin:auto; display:flex; align-items:center; justify-content:center; color:#000; font-size:1.5rem; margin-bottom:5px;">
                        <i class="fa-solid fa-comment"></i>
                    </div>
                    <span style="font-size:0.75rem;">카카오톡</span>
                </button>
                <!-- SMS -->
                <button onclick="handleShare('sms')" style="background:none; border:none; cursor:pointer; color:#fff;">
                    <div
                        style="background:#4cd137; width:50px; height:50px; border-radius:50%; margin:auto; display:flex; align-items:center; justify-content:center; color:#fff; font-size:1.5rem; margin-bottom:5px;">
                        <i class="fa-solid fa-envelope"></i>
                    </div>
                    <span style="font-size:0.75rem;">문자메세지</span>
                </button>
                <!-- Copy -->
                <button onclick="handleShare('copy')" style="background:none; border:none; cursor:pointer; color:#fff;">
                    <div
                        style="background:#6c5ce7; width:50px; height:50px; border-radius:50%; margin:auto; display:flex; align-items:center; justify-content:center; color:#fff; font-size:1.5rem; margin-bottom:5px;">
                        <i class="fa-solid fa-copy"></i>
                    </div>
                    <span style="font-size:0.75rem;">복사하기</span>
                </button>
            </div>
            <button onclick="closeShareModal()"
                style="margin-top: 1.5rem; background:none; border:1px solid #555; color:#888; padding:5px 15px; border-radius:20px; font-size:0.8rem;">닫기</button>
        </div>
    </div>

    <!-- Kakao Guide (Floating) -->
    <div id="kakaoGuide" class="kakao-guide">
        <p style="margin-top: 5px;">
            <span style="font-size: 1.2rem; color: #b8860b; margin-right: 5px;">✠</span>
            매일아침 묵상시 알림을 받으시려면<br>
            <strong>우측 하단 세로 점 3개(⋮)</strong>를 누르고<br>
            <span style="color: #fff; text-decoration: underline;">[다른 브라우저로 열기]</span>를 해주세요.
        </p>
        <button onclick="document.getElementById('kakaoGuide').style.display='none'"
            style="margin-top: 10px; background: none; border: 1px solid #555; color: #888; padding: 5px 12px; border-radius: 20px; font-size: 0.8rem;">
            닫기
        </button>
    </div>

    <!-- Permission Guide (General) -->
    <div id="notifGuide" class="kakao-guide" style="border-color: #ff4757;">
        <h3><span style="font-size: 1.2rem;">✠</span> 알림 설정 안내</h3>
        <p id="notifGuideText">
            매일아침 매일미사 묵상시를 받으시려면<br>
            알림 설정을 해주세요.
        </p>
        <button onclick="document.getElementById('notifGuide').style.display='none'"
            style="margin-top: 10px; background: none; border: 1px solid #555; color: #ccc; padding: 5px 12px; border-radius: 20px; font-size: 0.8rem;">
            확인
        </button>
    </div>

    <!-- Main Container -->
    <div class="card-container">
        <!-- Content Wrapper -->
        <div class="content-wrapper">
            <!-- App Header -->
            <div class="app-title">Good Morning Bona</div>

            <!-- Loading State -->
            <div id="loadingState" class="loading">
                <i class="fa-solid fa-spinner fa-spin fa-2x"></i>
                <div style="margin-top: 1rem;">오늘의 묵상을 불러오는 중...</div>
            </div>

            <!-- Error State -->
            <div id="errorState" class="error hidden">
                묵상 카드를 불러올 수 없습니다.<br>
                <button class="subscribe-btn" onclick="location.reload()" style="margin-top: 1.5rem;">
                    <i class="fa-solid fa-rotate-right"></i> 다시 시도
                </button>
            </div>

            <!-- Content State -->
            <div id="contentState" class="hidden" style="width: 100%;">
                <!-- Date -->
                <div id="dateDisplay" class="date-display"></div>

                <!-- Gospel Title -->
                <div id="gospelTitle" class="gospel-title">오늘의 말씀을 기다립니다</div>

                <!-- Image -->
                <div class="gold-frame-container">
                    <div class="gold-frame">
                        <img id="meditationImage" src="" alt="Meditation Image">
                    </div>
                </div>

                <!-- Meditation Body -->
                <div class="section-label">묵상시</div>
                <div id="meditationBody" class="meditation-body"></div>

                <!-- Prayer -->
                <div class="section-label">
                    오늘의 기도 <span style="font-size: 0.7rem; color: #888; font-weight: 400; margin-left: 5px;">{나의 기도로 수정
                        전달 가능}</span>
                </div>

                <!-- [Editable Prayer Text] -->
                <div id="prayerText" class="prayer-text" contenteditable="true" spellcheck="false" data-gramm="false">
                </div>
            </div>
        </div>

        <!-- Footer Wrapper -->
        <div class="fixed-footer-wrapper">
            <div class="button-row">
                <a href="https://missa.cbck.or.kr/DailyMissa" target="_blank" class="missa-link-btn">
                    매일미사 전문 보기
                </a>
                <button id="subscribeBtn" class="subscribe-btn">
                    매일아침 묵상카드 받기
                </button>
            </div>

            <div class="footer">Bona Studio</div>

            <button id="shareBtn" class="share-btn">
                전달하기
            </button>
        </div>
    </div>

    <!-- Server Data Injection -->
    <div id="server-data" style="display:none;" data-json="<%= JSON.stringify(staticData || null) %>"></div>

    <script>
        const VAPID_PUBLIC_KEY = '<%= vapidPublicKey %>';

        // 데이터 로드
        try {
            const rawData = document.getElementById('server-data').getAttribute('data-json');
            window.__STATIC_DATA__ = rawData ? JSON.parse(rawData) : null;
        } catch (e) {
            window.__STATIC_DATA__ = null;
        }

        async function loadTodaysMeditation() {
            try {
                let data = null;
                if (window.__STATIC_DATA__) {
                    data = window.__STATIC_DATA__;
                } else {
                    const urlParams = new URLSearchParams(window.location.search);
                    const targetDate = urlParams.get('date') || 'today';
                    const response = await fetch(`/bona/studio/api/post/${targetDate}`);
                    const result = await response.json();
                    if (result.success && result.data) data = result.data;
                    else throw new Error('No data');
                }

                if (data) {
                    const finalData = (data.content) ? { ...data, ...data.content } : data;
                    renderData(finalData);
                }
            } catch (error) {
                document.getElementById('loadingState').classList.add('hidden');
                document.getElementById('errorState').classList.remove('hidden');
            }
        }

        function renderData(data) {
            // Format date
            if (data.date) {
                const dateParts = data.date.split('-');
                const formattedDate = `✠ 가톨릭 매일아침 매일미사 복음묵상 ${dateParts[0]}. ${dateParts[1]}. ${dateParts[2]}.`;
                document.getElementById('dateDisplay').textContent = formattedDate;
            }

            document.getElementById('gospelTitle').textContent = data.one_line_message || '오늘의 말씀';
            document.getElementById('meditationBody').textContent = data.meditation_body || '';

            // [Custom Prayer Check]
            const urlParams = new URLSearchParams(window.location.search);
            let customPrayer = urlParams.get('prayer');
            const prayerEl = document.getElementById('prayerText');

            if (customPrayer) {
                // [Fix] URL에 밑줄이 포함되어 있다면 공백으로 복원해서 표시
                customPrayer = customPrayer.replace(/_/g, ' ');
                prayerEl.innerText = customPrayer;
                // 커스텀 기도 표시
                prayerEl.style.color = "#ebd095";
            } else {
                prayerEl.innerText = data.prayer_line || '- Bona Studio -';
            }

            // 원본 저장 (수정 여부 확인용)
            prayerEl.setAttribute('data-original', data.prayer_line || '');

            if (data.generated_image_url) {
                let imgUrl = data.generated_image_url;
                if (imgUrl.startsWith('/studio/')) imgUrl = '/bona' + imgUrl;

                const imgEl = document.getElementById('meditationImage');
                imgEl.src = imgUrl;
                imgEl.onclick = function () { openImageModal(this.src); };
                document.querySelector('.gold-frame-container').style.display = 'flex';
            } else {
                document.querySelector('.gold-frame-container').style.display = 'none';
            }

            document.getElementById('loadingState').classList.add('hidden');
            document.getElementById('contentState').classList.remove('hidden');
        }

        function openImageModal(src) {
            document.getElementById('zoomedImage').src = src;
            document.getElementById('imageModal').style.display = 'flex';
        }

        async function registerServiceWorker() {
            if ('serviceWorker' in navigator) {
                try {
                    return await navigator.serviceWorker.register('/bona/sw.js', { scope: '/bona/' });
                } catch (e) { }
            }
            return null;
        }

        async function subscribeUser() {
            const reg = await registerServiceWorker();
            if (!reg) return alert('서비스 워커 등록 실패');

            // Kakao Check
            const ua = navigator.userAgent.toUpperCase();
            if (ua.includes('KAKAOTALK')) {
                document.getElementById('kakaoGuide').style.display = 'block';
                // 렌더링 직후 확실하게 이동 (딜레이 + 강제 좌표)
                setTimeout(() => { window.scrollTo(0, 0); }, 100);
            }

            const permission = await Notification.requestPermission();
            if (permission === 'denied') {
                const guide = document.getElementById('notifGuide');
                document.getElementById('notifGuideText').innerHTML = '🚫 알림 권한이 차단되어 있습니다.<br><strong>[설정 > 사이트 설정 > 초기화]</strong> 후<br>다시 시도해주세요.';
                guide.style.display = 'block';
                return;
            }
            if (permission !== 'granted') {
                const guide = document.getElementById('notifGuide');
                document.getElementById('notifGuideText').innerHTML = '매일아침 매일미사 묵상시를 받으시려면<br>알림 설정을 해주세요.';
                guide.style.display = 'block';
                return;
            }

            try {
                const sub = await reg.pushManager.subscribe({
                    userVisibleOnly: true,
                    applicationServerKey: urlBase64ToUint8Array(VAPID_PUBLIC_KEY)
                });
                await fetch('/bona/studio/api/subscribe', {
                    method: 'POST',
                    body: JSON.stringify(sub),
                    headers: { 'Content-Type': 'application/json' }
                });
                alert('✅ 구독 완료! 매일 아침 묵상 카드를 받으실 수 있습니다.');
                const btn = document.getElementById('subscribeBtn');
                btn.innerHTML = '<i class="fa-solid fa-check"></i> 구독 완료';
                btn.disabled = true;
            } catch (e) {
                alert('구독 중 오류가 발생했습니다.');
            }
        }

        function urlBase64ToUint8Array(base64String) {
            const padding = '='.repeat((4 - base64String.length % 4) % 4);
            const base64 = (base64String + padding).replace(/-/g, '+').replace(/_/g, '/');
            const rawData = window.atob(base64);
            const outputArray = new Uint8Array(rawData.length);
            for (let i = 0; i < rawData.length; ++i) { outputArray[i] = rawData.charCodeAt(i); }
            return outputArray;
        }

        // [Share Button Click Hook]
        // [Share Button Click Hook]
        // [Share Button Click Hook]
        async function shareMeditation() {
            const data = prepareShareData();
            window.currentShareData = data;

            // 모달 열 때 미리 단축 링크 준비 (클립보드 동기화 동작 보장 위해)
            document.getElementById('shareModal').classList.remove('hidden');

            try {
                const res = await fetch('/bona/studio/api/shorten', {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({ date: data.date, prayer: data.prayer })
                });
                if (!res.ok) throw new Error('API Error');
                const json = await res.json();
                window.currentShareUrl = json.shortUrl || `https://uconai.ddns.net/bona/studio/s/${json.id}`;
            } catch (e) {
                console.error('Shorten Error:', e);
                window.currentShareUrl = `https://uconai.ddns.net/bona/studio/share?date=${data.date}&prayer=${encodeURIComponent(data.prayer)}`;
            }
        }

        function closeShareModal() {
            document.getElementById('shareModal').classList.add('hidden');
            checkSubscription();
        }

        function prepareShareData() {
            const urlParams = new URLSearchParams(window.location.search);
            // [Fix] KST 오늘 날짜 계산
            const kstDate = new Date(Date.now() + 1000 * 60 * 60 * 9).toISOString().split('T')[0];
            const date = urlParams.get('date') || kstDate;

            const prayerEl = document.getElementById('prayerText');
            // innerText가 현재 편집 중인 텍스트를 가장 잘 가져옴
            const prayer = (prayerEl.innerText || '').trim();
            const originalPrayer = (prayerEl.getAttribute('data-original') || '').trim();

            console.log('Prepared Prayer (Latest):', prayer);
            return { date, prayer, originalPrayer };
        }

        async function handleShare(type) {
            try {
                // 한 번 더 최신 데이터를 긁어와서 업데이트 (마지막에 수정한 내용 반영)
                const latestData = prepareShareData();
                window.currentShareData = latestData;

                const data = window.currentShareData;
                if (!data) {
                    alert('공유할 데이터를 찾을 수 없습니다.');
                    return;
                }

                const prayer = data.prayer;
                const shareUrl = window.currentShareUrl || `https://uconai.ddns.net/bona/studio/share?date=${data.date}&prayer=${encodeURIComponent(prayer)}`;
                const shareText = `{가톨릭} 매일아침 매일미사 복음묵상\n\n"${prayer}"\n\n오늘의 묵상기도 카드를 확인하세요\n${shareUrl}`;

                console.log('Sharing type:', type, 'Prayer:', prayer, 'URL:', shareUrl);

                if (type === 'kakao') {
                    await copyToClipboard(shareText);
                    const isMobile = /iPhone|iPad|iPod|Android/i.test(navigator.userAgent);
                    if (isMobile) {
                        alert('✅ 내용이 복사되었습니다.\n카카오톡을 열어 [붙여넣기] 하세요.');
                        window.location.href = 'kakaotalk://';
                    } else {
                        alert('✅ 내용이 복사되었습니다.\n카카오톡에 [붙여넣기] 하세요.');
                    }
                } else if (type === 'sms') {
                    await copyToClipboard(shareText);
                    const isMobile = /iPhone|iPad|iPod|Android/i.test(navigator.userAgent);
                    if (isMobile) {
                        alert('✅ 내용이 복사되었습니다.\n문자 앱을 열어 [붙여넣기] 하세요.');
                        window.location.href = 'sms:';
                    } else {
                        alert('✅ 내용이 복사되었습니다.\n문자 앱에 [붙여넣기] 하세요.');
                    }
                } else if (type === 'copy') {
                    const success = await copyToClipboard(shareText);
                    if (success) {
                        alert('✅ 공유 메시지가 복사되었습니다.\n원하는 곳에 붙여넣기 하세요.');
                    } else {
                        alert('❌ 복사에 실패했습니다.\n수동으로 복사해주세요.');
                    }
                }
            } catch (err) {
                console.error('Share Error:', err);
                alert('오류가 발생했습니다: ' + err.message);
            }
            closeShareModal();
        }

        async function copyToClipboard(text) {
            // Modern API
            if (navigator.clipboard && navigator.clipboard.writeText) {
                try {
                    await navigator.clipboard.writeText(text);
                    return true;
                } catch (e) {
                    console.warn('Navigator clipboard failed, trying fallback');
                }
            }

            // Fallback (Textarea)
            const t = document.createElement("textarea");
            t.value = text;
            t.style.position = 'fixed';
            t.style.left = '-9999px';
            t.style.top = '0';
            t.setAttribute('readonly', ''); // Mobile keyboard prevention
            document.body.appendChild(t);
            t.focus();
            t.select();
            t.setSelectionRange(0, 99999); // iOS support

            let successful = false;
            try {
                successful = document.execCommand('copy');
            } catch (err) {
                console.error('Fallback copy failed', err);
            }

            document.body.removeChild(t);
            return successful;
        }

        function checkSubscription() {
            const isSubscribed = localStorage.getItem('bona_subscribed') === 'true';
            const hasPermission = Notification.permission === 'granted';
            if (!isSubscribed && !hasPermission) {
                setTimeout(() => {
                    if (confirm("매일 아침 묵상 카드를 받아보시겠습니까?")) subscribeUser();
                }, 500);
            }
        }

        // [New] Real-time URL Sync for Data Persistance
        // 기도문을 수정하면 즉시 주소창(URL)에 반영 -> '다른 브라우저로 열기' 해도 내용 유지됨
        function updateUrlWithPrayer() {
            const prayerEl = document.getElementById('prayerText');
            const currentPrayer = prayerEl.innerText.trim();
            const originalPrayer = (prayerEl.getAttribute('data-original') || '').trim();

            const url = new URL(window.location);

            if (currentPrayer && currentPrayer !== originalPrayer) {
                url.searchParams.set('prayer', currentPrayer);
            } else {
                url.searchParams.delete('prayer');
            }

            // 주소창만 조용히 변경 (새로고침 X)
            window.history.replaceState({}, '', url);
        }

        // Debounce setup
        let debounceTimer;
        document.getElementById('prayerText').addEventListener('input', function () {
            clearTimeout(debounceTimer);
            debounceTimer = setTimeout(updateUrlWithPrayer, 300); // 0.3초 딜레이
        });

        document.getElementById('subscribeBtn').addEventListener('click', subscribeUser);
        document.getElementById('shareBtn').addEventListener('click', shareMeditation);
        window.addEventListener('DOMContentLoaded', loadTodaysMeditation);
    </script>
</body>

</html>