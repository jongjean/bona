# Good Morning Bona - Release v0.4 (Stabilization Version)

본 릴리즈는 사용자 설계 철학에 따른 **"개발 엔진(Development)과 웹 서비스(WWW)의 철저한 격리 및 자동화"**를 목표로 한 4단계 구조화 작업의 결과물입니다.

## 🚀 주요 업데이트 사항

### 1. 데이터 및 자산 통합 (Phase 1)
- **구독자 DB 단일화**: `/home/ucon/bona/data/subs.json`을 마스터 DB로 확정하고 정합성 확보.
- **이미지 자산 수집**: 흩어져 있던 과거 묵상 이미지 및 데이터를 내부 `public` 경로로 통합.
- **서비스 자원 이관**: `sw.js` 및 리더용 자산들을 `studio/public`으로 이동하여 관리 일원화.

### 2. 엔진 독립성 및 경로 정상화 (Phase 2)
- **정적 자산 매핑**: 모든 외부 정적 파일 참조를 내부 엔진의 `/studio/` 가상 경로로 수정.
- **외부 종속성 제거**: `app.js`에서 `/var/www/bona`를 직접 참조하여 서비스하던 보안 취약점 제거.
- **데이터 경로 고정**: 모든 읽기/쓰기 작업을 `/home/ucon/bona` 내부 데이터 디렉토리로 고립.

### 3. 자동화 배포 엔진 구축 (Phase 3)
- **Atomic Deployment**: `deployToWebFolder()` 함수를 통해 `/var/www/bona`를 매일 아침 안전하고 원자적으로 갱신.
- **스케줄러 통합**: 매일 아침 06:00 묵상 발송 직후 자동 배포 동기화 로직 탑재.
- **수동 배포 API**: 관리자 페이지 전용 [웹 서비스 동기화] 버튼 및 API 구현.

### 4. 웹 서비스 폴더 대청소 및 보안 격리 (Phase 4)
- **독소 폴더 삭제**: `/var/www/bona` 내에 존재하던 백엔드 소스(`studio/`), 데이터(`data/`), 백업 파일 전량 제거.
- **서비스 창구화**: 웹 폴더를 "언제든 삭제해도 1초 만에 복구 가능한" 순수 정적 창구로 재정의.

### 5. 버그 수정 및 UX 최적화
- **캘린더 내비게이션**: 이전/다음/오늘 버튼을 AJAX 방식으로 전환하여 페이지 새로고침 없는 부드러운 이동 지원.
- **경로 문제 해결**: 프록시(Caddy) 환경에서의 경로 인식을 개선하여 모든 버튼 작동 무결성 확보.

---
**Status**: Stabilized (Phase 4 Completed)
**Version**: v0.4
**Author**: Antigravity (AI Core)
